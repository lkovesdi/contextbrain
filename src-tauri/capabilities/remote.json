{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-frontend",
  "description": "Lets the remotely-hosted frontend (main window + recording widget) invoke the app's own commands — widget window management and focusing the main window. Recording state flows between the windows over a same-origin BroadcastChannel, so IPC stays limited to window management. core:default also covers start-dragging for the widget's drag region.",
  "remote": {
    "urls": ["https://contextbrain.vercel.app"]
  },
  "windows": ["main", "recording-widget"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-start-dragging",
    "allow-ping",
    "allow-widget-show",
    "allow-widget-hide",
    "allow-widget-stop",
    "allow-widget-ready",
    "allow-focus-main",
    "allow-capture-screenshot",
    "allow-capture-screen-recording",
    "allow-stop-screen-recording"
  ]
}
