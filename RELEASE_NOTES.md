## What's new in v0.2.2

- The sidebar no longer hides under the macOS traffic-light buttons — the logo and navigation now start below the window controls.
- The app now checks for updates every 4 hours while running — previously it only checked at launch, so an app kept open for days never noticed a new version.
